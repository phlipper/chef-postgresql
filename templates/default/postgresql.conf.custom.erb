# generated by Chef for node <%= node["fqdn"] %>
#
# -----------------------------
# PostgreSQL configuration file
# -----------------------------
#

<% node['postgresql']['conf'].each_pair do |key, value| %>
<%= "#{key} = '#{value}'" %>
<% end %>
